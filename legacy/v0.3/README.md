# Legacy v0.3 Archive

This folder preserves the old Tortoise-owned ERC-1155 implementation, its interface,
and its V1-specific tests/scripts for historical reference and migration reasoning.

These files are not part of the active Foundry `src`, `test`, or `script` paths.
Current development and deployment should target `TortoiseMintRouter` and
`TortoiseShell`.
