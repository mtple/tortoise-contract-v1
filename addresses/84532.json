{
  "_comment": "Base Sepolia (chainId 84532) ETH-stack address registry. inProcessFactory is INTENTIONALLY ABSENT: the In Process Sepolia factory is unconfirmed, so the creation scripts FAIL CLOSED (Config.inProcessFactory reverts) until it is pinned here or passed via INPROCESS_FACTORY. Do NOT use the canonical Zora factory unless Tortoise deliberately chooses a non-In-Process testnet stack. See planning/setup-actions-reference.md C.1 and the plan's Base Sepolia deployment section.",
  "stakingToken": "0x1c3879b9dabA1B51253b109726C44bb391cae8c5"
}
